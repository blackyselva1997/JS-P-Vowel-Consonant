let alpha = String(prompt("Enter a Alphabet only one"));

if (alpha === "a" || alpha === "e" || alpha === "i" || alpha === "o" || alpha === "u" || alpha === "A" || alpha === "E" || alpha === "I" || alpha === "O" || alpha === "U") {
    document.write("You enter a Vowel");
} else {
    document.write("You enter a consonant");
}